# NeRF-pytorch

To run NeRF-pytorch model:
```
bash scripts/bottles_v1.sh
```
Feel free to create new config files under `.config`, and modify the config file name correspondingly in script files under `.scripts`.
